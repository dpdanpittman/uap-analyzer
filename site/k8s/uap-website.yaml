# uap-analyzer marketing site — nginx serving the Astro build output.
# Mirrors swarm-website / memory-website deployment shape.
#
# How it gets served:
#   Caddy on zaphod → uap.mabus.ai → reverse_proxy localhost:3404
#   localhost:3404 → this pod's nginx → /usr/share/nginx/html (mounted from hostPath)
#
# Build flow (on zaphod):
#   cd ~/src/uap-analyzer/site && npm install && npm run build
#
# Apply (from k8s-control):
#   sudo kubectl --kubeconfig=/etc/kubernetes/admin.conf apply -f uap-website.yaml

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: uap-website-nginx
  namespace: zaphod
data:
  default.conf: |
    server {
      listen 80;
      server_name _;
      root /usr/share/nginx/html;
      index index.html;

      location / {
        try_files $uri $uri/ $uri/index.html =404;
      }

      gzip on;
      gzip_types text/plain text/css application/javascript application/json image/svg+xml;
      gzip_min_length 256;

      location ~ \.(css|js|woff2?|svg|png|jpg|jpeg|webp|ico)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
      }
    }

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: uap-website
  namespace: zaphod
  labels:
    app: uap-website
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: uap-website
  template:
    metadata:
      labels:
        app: uap-website
    spec:
      nodeSelector:
        kubernetes.io/hostname: zaphod
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - name: http
              containerPort: 80
              hostPort: 3404
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 30
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 64Mi
          volumeMounts:
            - name: site-content
              mountPath: /usr/share/nginx/html
              readOnly: true
            - name: nginx-config
              mountPath: /etc/nginx/conf.d
              readOnly: true
      volumes:
        - name: site-content
          hostPath:
            path: /home/zaphod-beeblebox/src/uap-analyzer/site/dist
            type: Directory
        - name: nginx-config
          configMap:
            name: uap-website-nginx

---
apiVersion: v1
kind: Service
metadata:
  name: uap-website
  namespace: zaphod
  labels:
    app: uap-website
spec:
  type: ClusterIP
  selector:
    app: uap-website
  ports:
    - port: 80
      targetPort: 80
